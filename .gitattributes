# Auto detect text files and perform LF normalization
* text=auto

# Treat binary assets as binary so line-ending normalization never corrupts them
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.pdf binary
